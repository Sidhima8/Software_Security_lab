import mysql.connector

def connect_db():
    username = "admin"
    password = "Admin@123"

    return mysql.connector.connect(
        host="localhost",
        user=username,
        password=password
    )
